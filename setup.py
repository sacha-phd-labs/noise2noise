from setuptools import setup, find_packages

setup(
    name="noise2noise",
    version="0.1.0",
    description="Noise2Noise PET image denoising model",
    packages=find_packages(),
    python_requires=">=3.8",
    # install_requires=[
    #     "torch>=1.9.0",
    #     "pytorcher",
    # ],
)
